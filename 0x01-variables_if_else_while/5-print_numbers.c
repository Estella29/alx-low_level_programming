#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - prints single digit numbers, followed by a new line.
* Return: Always 0 (Success)
*/
int main(void)
{
	int i;

	for (i=0; i<=10; i++)
	{
		printf("%d\n", i);
	}

	return (0);
}
